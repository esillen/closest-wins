version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - ADMIN_PASSWORD=admin123
      - THYMELEAF_CACHE=false
    restart: unless-stopped

